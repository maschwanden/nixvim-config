{
  plugins = {
    web-devicons = {
      enable = true;
    };

    # Buffer bar
    bufferline = {
      enable = true;
    };

    # Status bar
    lualine = {
      enable = true;
    };

    # Make `nvim .` look prettier, i.e. it prettifies the file tree view of nvim
    oil = {
      enable = true;
    };
  };
}
